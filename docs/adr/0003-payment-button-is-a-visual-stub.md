# מסך/כפתור תשלום הוא stub ויזואלי בלבד — בלי שום לוגיקה מאחוריו

**Status:** accepted (מחליף ניסוח קודם של ההחלטה הזו)

מסלול "רכישת תמהיל + הוצאת אישורים" כולל לפי המסמך "מסך תשלום" לפני המשך התהליך. לצורך ההאקתון, השלב הזה הוא **כפתור ריק** שמקדם למסך הבא — **בלי** Cloud Function, בלי כתיבה ל-Firestore, בלי שום סטטוס תשלום נשמר. לא "מדומה עם תיעוד", פשוט לא קיים מאחורי ה-UI.

**הנימוק:** אין ערך בלבנות ולו סימולציה דקה של תשלום בחלון הזמן של ההאקתון — המחוון מדגיש מיקוד בעיקר (25% "הצגת העיקר מתוך הדמו"), והעיקר כאן הוא חווית המשתמש בשאלון/בהשוואה, לא תשתית תשלומים. השארת הכפתור (לא הסרתו) חשובה כדי שהזרימה המוצגת בדמו תיראה רציפה ומלאה.

**להמשך (אחרי ההאקתון):** מאחורי הכפתור הזה יתחבר Cloud Function אמיתי + ספק סליקה (PayPlus מומלץ לשוק הישראלי) — אין עדיין חוזה API לשמר כי אין עדיין לוגיקה.